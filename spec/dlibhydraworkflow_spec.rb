require 'spec_helper' 

describe Dlibhydraworkflow do
  it 'has a version number' do
    expect(Dlibhydraworkflow::VERSION).not_to be nil
  end
  it 'does something useful' do
    expect(true).to eq(true)
  end 
end
